using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UINeedle : MonoBehaviour
{
    /*
       public TMP_Text rpmText;
       public TMP_Text gearText;
       public Transform rpmNeedle;

       public float minNeedleRot;
       public float maxNeedleRot;
   */
    // Start is called before the first frame update


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        //rpmText.text = engineRPM + "RPM";
        //gearText.text = gearState == GearState.Neutral ? "N" : (currentGearNum + 1).ToString();
        //rpmNeedle.rotation = Quaternion.Euler(0, 0, Mathf.Lerp(minNeedleRot, maxNeedleRot, engineRPM / (redLine * 1.1f)));
    }
}
